{
  "name": "slack-cli-napi",
  "version": "0.1.0",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "napi build --release",
    "build:debug": "napi build"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3"
  },
  "napi": {
    "binaryName": "slack-cli",
    "targets": [
      "aarch64-apple-darwin"
    ]
  }
}
